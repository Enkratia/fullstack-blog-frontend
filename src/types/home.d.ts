type UsMissionType = {
  about: {
    title: string;
    description: string;
  };
  mission: {
    title: string;
    description: string;
  };
};

// **
type WhyWeStartedType = {
  subtitle: string;
  title: string;
  description: string;
};

// **
type FeaturedCompanyType = {
  id: number;
  title: string;
  imageUrl: string;
  linkUrl: string;
};

type TestimonialStaticType = {
  subtitle: strig;
  title: strig;
  description: strig;
};

// **
type TestimonialType = {
  id: number;
  text: type;
  imageUrl: type;
  fullName: type;
  address: type;
};
